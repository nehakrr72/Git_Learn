name: Workflow-3

on:
  workflow_dispatch

jobs:
  job-1:
    name: Print Messages Job
    runs-on: ubuntu-24.04

    steps:
      - name: Print Welcome-Message
        run: |
          echo "message1!"

      - name: Print Thank-You-Message
        run: |
          echo "message2!"
